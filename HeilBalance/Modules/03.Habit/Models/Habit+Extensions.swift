//import Foundation
//
//extension Habit {
////
////
////    /// 总打卡次数
////    var completedCountDynamic: Int {
////        return completedDateSet.count
////    }
////
////    // 可选：跳过相关方法
////    func markSkipped(for date: Date = Date()) {
////        let day = Calendar.current.startOfDay(for: date)
////        var dates = skippedDateSet
////        dates.insert(day)
////        skippedDateSet = dates
////    }
////
////    func isSkipped(on date: Date) -> Bool {
////        let day = Calendar.current.startOfDay(for: date)
////        return skippedDateSet.contains(day)
////    }
//}
